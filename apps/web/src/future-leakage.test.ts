import {execFileSync,spawn} from "node:child_process";import {readdirSync,readFileSync,statSync} from "node:fs";import {homedir} from "node:os";import {resolve} from "node:path";import {chromium} from "@playwright/test";import {expect,test} from "vitest";import {FUTURE_MARKER} from "@daily/test-data/future-marker";
test("built PWA and real Cache Storage contain no future marker",async()=>{const root=resolve(import.meta.dirname,"..");execFileSync("pnpm",["build"],{cwd:root,stdio:"pipe"});const files=(dir:string):string[]=>readdirSync(dir).flatMap(name=>{const p=resolve(dir,name);return statSync(p).isDirectory()?files(p):[p]}),dist=resolve(root,"dist");for(const file of files(dist))expect(readFileSync(file).toString()).not.toContain(FUTURE_MARKER);const server=spawn("python3",["-m","http.server","4179","--directory",dist],{stdio:"ignore"});try{await new Promise(r=>setTimeout(r,500));const executablePath=resolve(homedir(),"Library/Caches/ms-playwright/chromium-1217/chrome-mac-arm64/Google Chrome for Testing.app/Contents/MacOS/Google Chrome for Testing"),browser=await chromium.launch({headless:true,executablePath});try{const page=await browser.newPage();await page.goto("http://127.0.0.1:4179",{waitUntil:"networkidle"});await page.evaluate(()=>navigator.serviceWorker.ready);const cached=await page.evaluate(async()=>{const bodies=[];for(const name of await caches.keys()){const cache=await caches.open(name);for(const request of await cache.keys())bodies.push(await (await cache.match(request))!.text())}return bodies});expect(cached.length).toBeGreaterThan(0);for(const body of cached)expect(body).not.toContain(FUTURE_MARKER)}finally{await browser.close()}}finally{server.kill("SIGTERM")}},20_000);
